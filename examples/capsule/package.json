{
  "name": "starter-capsule",
  "version": "1.0.0",
  "description": "You can use this project to bootstrap your own one.",
  "scripts": {
    "start": "tsc && rm src/index.mjs && cp src/index.js src/index.mjs -f  && node  --experimental-specifier-resolution=node src/index.mjs"
  },
  "dependencies": {
    "@bytecodealliance/preview2-shim": "^0.16.5",
    "node-fetch": "^2.6.1",
    "ternoa-js": "file:../../build",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.7"
  }
}
